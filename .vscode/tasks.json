{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "command": "export NVM_DIR=\"$HOME/.nvm\" && [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\"; npm run compile",
      "problemMatcher": [
        "$tsc"
      ],
      "group": "build",
      "label": "npm: compile"
    }
  ]
}
